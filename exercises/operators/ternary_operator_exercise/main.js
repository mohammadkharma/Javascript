// 1
let age = 20;
let canDrive = (age > 18) ? "yes can drive" : "no, too young!";
console.log(canDrive); // yes can drive

// 2
let speed = 140;
let speedCheck = (speed > 130) ? "you're going too fast!" : "You're driving below the speed limit";
console.log(speedCheck); //  // you're going too fast!

// 3
let personAge = (age < 21) ? "serve juice" : "serve wine";
console.log(personAge); // serve juice

// 4
let student = true; 
let isStudent = (student == true) ? "Ticket costs €5,00" : "Ticket costs €12,00";
console.log(isStudent); // Ticket costs €5,00

// 5
let timeOfDay = 11;
let time = (timeOfDay < 12) ? "Good Morning" : "Good Evening";
console.log(time); // Good Morning